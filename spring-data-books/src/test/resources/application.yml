spring:
  datasource:
    url: jdbc:h2:mem:testdb222;MODE=MySQL;
  sql:
    init:
      mode: always
      data-locations: data.sql
#      schema-locations: schema.sql
  shell:
    interactive:
      enabled: false
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: false


