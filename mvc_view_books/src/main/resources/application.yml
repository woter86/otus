spring:
  datasource:
    url: jdbc:h2:mem:testdb212;MODE=MySQL;
  sql:
    init:
      mode: always


  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true


logging:
  level:
    ROOT: ERROR